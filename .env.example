# The official Hermes API Server listener, not `hermes serve` dashboard mode.
HERMES_API_URL=http://127.0.0.1:8642

# Server-side only. Never prefix this with VITE_ and never commit a real value.
HERMES_API_KEY=

# Optional Agent OS port (default 4177).
PORT=4177
